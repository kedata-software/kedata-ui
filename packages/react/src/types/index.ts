export type PropsGetterParams = {
  className?: string;
  style?: object;
};

export type TestableProps = {
  'data-testid'?: string;
};

export type WithFieldProps = {
  name?: string;
  disabled?: boolean;
  invalid?: boolean;
  readOnly?: boolean;
};
