const switchParts = {
  root: 'switch',
  control: 'switch__control',
  thumb: 'switch__thumb',
  label: 'switch__label',
  hiddenInput: 'switch__hidden-input',
} as const;

export default switchParts;
