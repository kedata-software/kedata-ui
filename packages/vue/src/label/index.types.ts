export type LabelProps = {
  class?: string;
  required?: boolean;
  showAsterisk?: boolean;
  withParts?: boolean;
};

export type WithLabelProps = {
  required?: boolean;
  showAsterisk?: boolean;
};
