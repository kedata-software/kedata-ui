export type LabelProps = {
  class?: string;
  required?: boolean;
  /**
   * @default true
   */
  showAsterisk?: boolean;
  withParts?: boolean;
};

export type WithLabelProps = {
  required?: boolean;
  showAsterisk?: boolean;
};
