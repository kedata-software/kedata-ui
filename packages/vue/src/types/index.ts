export type PropsGetterParams = {
  class?: string;
};

export type TestableProps = {
  "data-testid"?: string;
};

export type WithFieldProps = {
  name?: string;
  disabled?: boolean;
  invalid?: boolean;
  readOnly?: boolean;
};
